#example how to run

mpirun -np 5 ./arc_length_interface \
  -my_file PeelTest.cub -ksp_type fgmres \
  -pc_type asm -sub_pc_type lu \
  -ksp_monitor ksp_monitor \
  -ksp_atol 1e-10 -ksp_rtol 1e-10 \
  -snes_monitor \
  -snes_type newtonls -snes_linesearch_type l2 \
  -snes_linesearch_monitor \
  -snes_max_it 16 -snes_atol 1e-8 -snes_rtol 1e-8 \
  -my_sr 1e-3 -my_ms 10 -snes_converged_reason

#input

PeelTest.cub - input mesh consisting SIDESET 1 (consiting faces where dirichlet
bc. are applied), SIDESET2 (faces where tractions t = [0,1,0], are applied),
SIDESET 3 (faces where tractions t = [0,-1,0], are applied).  SIDESET 4 consist
faces where interfaces element should be inserted.


#Output

out.vtk - computational mesh
out_post_proc.vtk - refinded mesh of TETs for prostrocesing
