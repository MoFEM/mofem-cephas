Steps
==================================================================
1. homogenisation of thermal RVE            (save output to thermal_Dmat.out binary file)
2. homogenisation of moisture diffusion RVE (save output to diffusion_Dmat.out binary file)

1. Run thermal_unsteady save output as solution.h5m and out_.vtk (conductivity for this is read from thermal_Dmat.out)
2. Run moisture_unsteady save output as solution_mois.h5m and and Conc_.vtk (diffusivity for this is read from diffusion_Dmat.out )
3. Run calculate_wt



Example computational homogenisation for moisture transport
(Here both MoistureTrans_Disp_MRhs is used for both thermal and diffusion problem)
============================================================
make && mpirun -np 1 ./MoistureTrans_Disp_MRhs -my_file ../users_modules/degradation_model/meshes/RVE_Cube_moisture.cub -ksp_type fgmres -pc_type lu -pc_factor_mat_solver_package superlu_dist -ksp_monitor -my_output_file_Dmat diffusion_Dmat.out -my_order 1

make && mpirun -np 1 ./MoistureTrans_Disp_MRhs -my_file ../users_modules/degradation_model/meshes/RVE_Cube_thermal.cub -ksp_type fgmres -pc_type lu -pc_factor_mat_solver_package superlu_dist -ksp_monitor -my_output_file_Dmat thermal_Dmat.out -my_order 1


Unsteady thermal macro problem
============================================================
make thermal_unsteady_inputDmat && mpirun -np 1 ./thermal_unsteady_inputDmat \
-my_file ../users_modules/degradation_model/meshes/macro.cub \
  -ksp_type fgmres -pc_type lu -pc_factor_mat_solver_package superlu_dist -ksp_monitor \
  -snes_type newtonls -snes_linesearch_type basic -snes_max_it 100 -snes_atol 1e-8 -snes_rtol 1e-8 -snes_monitor \
  -ts_monitor -ts_type beuler -ts_dt 0.1 -ts_final_time 5 -snes_lag_jacobian -2 \
  -my_order 1 -my_max_post_proc_ref_level 1


Unsteady moisture macro problem
============================================================
make diffusion_unsteady_inputDmat && mpirun -np 1 ./diffusion_unsteady_inputDmat \
-my_file solution.h5m \
  -ksp_type fgmres -pc_type lu -pc_factor_mat_solver_package superlu_dist -ksp_monitor \
  -snes_type newtonls -snes_linesearch_type basic -snes_max_it 100 -snes_atol 1e-8 -snes_rtol 1e-8 -snes_monitor \
  -ts_monitor -ts_type beuler -ts_dt 0.1 -ts_final_time 5 -snes_lag_jacobian -2 \
  -my_order 1 -my_max_post_proc_ref_level 1


calculate_wt used to calculate degredaiton (1-wt)
============================================================
make wt_unsteady_macro && mpirun -np 1 ./wt_unsteady_macro \
-my_file solution_mois.h5m \
  -ksp_type fgmres -pc_type lu -pc_factor_mat_solver_package superlu_dist -ksp_monitor \
  -snes_type newtonls -snes_linesearch_type basic -snes_max_it 100 -snes_atol 1e-8 -snes_rtol 1e-8 -snes_monitor \
  -ts_monitor -ts_type beuler -ts_dt 0.1 -ts_final_time 5 -snes_lag_jacobian -2 \
  -my_order 1 -my_max_post_proc_ref_level 1 \
  -my_saturation_conc 5


Mechanical problem
============================================================
make mech_macro && mpirun -np 1 ./mech_macro \
-my_file_Macro solution_wt.h5m -my_file_RVE ../users_modules/degradation_model/meshes/RVE_Cube_mech.cub\
  -ksp_type fgmres -pc_type lu -pc_factor_mat_solver_package superlu_dist -ksp_monitor \
  -my_order 1 -my_max_post_proc_ref_level 1 \









