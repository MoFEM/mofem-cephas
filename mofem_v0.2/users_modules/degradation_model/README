Steps
==================================================================
1. Run thermal_unsteady_macro save output as solution_temp.h5m
2. Run moisture_unsteady_macro save output as solution_mois.h5m
3. Run calculate_wt



Unsteady thermal macro problem
============================================================
make thermal_unsteady_macro && mpirun -np 2 ./thermal_unsteady_macro \
-my_file ../users_modules/degradation_model/meshes/macro.cub \
  -ksp_type fgmres -pc_type lu -pc_factor_mat_solver_package superlu_dist -ksp_monitor \
  -snes_type newtonls -snes_linesearch_type basic -snes_max_it 100 -snes_atol 1e-8 -snes_rtol 1e-8 -snes_monitor \
  -ts_monitor -ts_type beuler -ts_dt 0.1 -ts_final_time 5 -snes_lag_jacobian -2 \
  -my_order 1 -my_max_post_proc_ref_level 1


Unsteady moisture macro problem
============================================================
make moisture_unsteady_macro && mpirun -np 2 ./moisture_unsteady_macro \
-my_file solution_temp.h5m \
  -ksp_type fgmres -pc_type lu -pc_factor_mat_solver_package superlu_dist -ksp_monitor \
  -snes_type newtonls -snes_linesearch_type basic -snes_max_it 100 -snes_atol 1e-8 -snes_rtol 1e-8 -snes_monitor \
  -ts_monitor -ts_type beuler -ts_dt 0.1 -ts_final_time 5 -snes_lag_jacobian -2 \
  -my_order 1 -my_max_post_proc_ref_level 1


calculate_wt
============================================================
make && mpirun -np 1 ./wt_unsteady_macro \
-my_file solution_mois.h5m \
  -ksp_type fgmres -pc_type lu -pc_factor_mat_solver_package superlu_dist -ksp_monitor \
  -snes_type newtonls -snes_linesearch_type basic -snes_max_it 100 -snes_atol 1e-8 -snes_rtol 1e-8 -snes_monitor \
  -ts_monitor -ts_type beuler -ts_dt 0.1 -ts_final_time 5 -snes_lag_jacobian -2 \
  -my_order 1 -my_max_post_proc_ref_level 1 \
  -my_saturation_conc 5



