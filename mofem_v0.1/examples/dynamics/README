Check before run
================

* Check where is your mesh file
* Check what verion of openmoi (or other MPI library) are you using

Example run
===========
mpirun -np 4 ./linear_dynamics_elasticity -my_file LShapeCoarse.cub -ksp_type fgmres -ksp_gmres_restart 1000 -pc_type asm -sub_pc_type lu -ksp_atol 1e-10 -ksp_rtol 1e-10 -snes_monitor -snes_type newtonls -snes_linesearch_type basic -snes_max_it 100 -snes_atol 1e-8 -snes_rtol 1e-8 -ts_monitor -ts_type alpha -ts_dt 0.01 -ts_final_time 50  | tee log


More options
=================

-my_disp_order 2 - sets order of approx. for displacements 
-my_velocity_order 1 - sets order of approx. for velocities

-my_max_pot_proc_ref_level 2 - sets order of refinment for postporocessing mesh
