Check before run
================

* Check where is your mesh file
* Check what verion of openmoi (or other MPI library) are you using

Example run
===========


mpirun -np 4 ./laplacian_problem -my_file pipe.cub -ksp_type fgmres -pc_type lu -pc_factor_mat_solver_package superlu_dist -ksp_monitor -my_order 1

mpirun -np 4 ./potential_flow -my_file airfoil.cub -ksp_type fgmres -pc_type lu -pc_factor_mat_solver_package superlu_dist -ksp_monitor -my_order_laplacian 1 -my_order_preassure 1

./calculate_forces -my_file solution_pressure.h5m



