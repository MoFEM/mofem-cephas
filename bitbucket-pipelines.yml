image: likask/mofem-spack-build:latest

pipelines:
  default:
    - step:
        script:
          - echo PWD $PWD
          - git submodule update --init mofem/users_modules
          - export MOFEM_SRC_DIR=$PWD
          - echo MOFEM_SRC_DIR $MOFEM_SRC_DIR
          - echo "Install with spack"
