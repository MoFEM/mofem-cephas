Check before run
================

* Check where is your mesh file
* Check what verion of openmoi (or other MPI library) are you using

Example run
===========

For 1st Fibre use potential_flow_rope
mpirun -np 4 ./potential_flow_rope -my_file ~/Documents/PhD/moFEM/mofem_v0.0.1/meshes/new_clamped_wire.cub -ksp_type fgmres -ksp_gmres_restart 1000 -pc_type asm -sub_pc_type lu -ksp_monitor -my_order 1

For 2nd and consecutive Fibres use potential_flow_rope_2nd
Specify the new Blockset to solve the potential flow for, and one can set a new output_file name (for separate outputs)
mpirun -np 4 ./potential_flow_rope_2nd -my_file solution.h5m -block_for_potential PotentialFlow1 -output_file out1.vtk -ksp_type fgmres -ksp_gmres_restart 1000 -pc_type asm -sub_pc_type lu -ksp_monitor -my_order 1