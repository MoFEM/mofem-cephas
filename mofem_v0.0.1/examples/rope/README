#Generate fibre direction in Model

mpirun -np 4 ./potential_flow_rope -my_file new_clamped_wire.cub -ksp_type fgmres -ksp_gmres_restart 1000 -pc_type asm -sub_pc_type lu -ksp_monitor -my_order 1

#example how to run the actual problem

mpirun -np 3 ./wire_interface -my_file solution.h5m -ksp_type fgmres -pc_type asm -sub_pc_type lu -ksp_monitor 

#input

3threadMesh.cub - input mesh consisting SideSet 1 (consiting faces where dirihlet
bc. are applied), SideSet2 (faces where tractions t = [0,1,0], are applied),
SideSet 3 could be set ad a Neuman BC but for this mesh is left empty,
SideSet 4,5,6 are the face between the 3 fibres.


#Output

out.vtk - computational mesh
out_post_proc.vtk - refinded mesh of TETs for prostrocesing
out_post_proc_prisms.vtk - refinded mesh of PRISMs for prostrocesing
