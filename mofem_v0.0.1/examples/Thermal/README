Check before run
================

* Check where is your mesh file
* Check what verion of openmoi (or other MPI library) are you using

Example run
===========
make && mpirun -np 1 ./Thermal -my_file ~/Documents/Post-Doc\ Data/MoFEM\ Examples/Thermal/Thermal_Exp1.cub -ksp_type fgmres -ksp_gmres_restart 30 -pc_type asm -sub_pc_type lu -ksp_monitor