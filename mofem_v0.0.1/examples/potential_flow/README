Check before run
================

* Check where is your mesh file
* Check what verion of openmoi (or other MPI library) are you using

Example run
===========

mpirun -np 4 ./potential_flow -my_file pipe.cub -ksp_type fgmres -ksp_gmres_restart 1000 -pc_type asm -sub_pc_type lu -ksp_monitor -my_order 5
